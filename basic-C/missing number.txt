#include <stdio.h>
int main(){
	int n;
	scanf("%d", &n);
	int arr[n];
	for(int i=0;i<n;i++){
		scanf("%d", &arr[i]);
	}
	int val;
	for(int i=0;i<n;i++){
		if(arr[i]+1 !=arr[i+1]){
			val = arr[i]+1;
			break;
		}
	}
	printf("The Missing Number is: %d", val);
	return 0;
}